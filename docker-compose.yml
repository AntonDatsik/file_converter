version: '3.7'

services:
  web:
    image: pdf_generator:latest
    build:
      context: .
    command:  python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./:/app
    ports:
      - 8000:8000
    environment:
      - DEBUG=1
